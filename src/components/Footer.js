import React, { Component } from 'react'

export default class Footer extends Component {
  render () {
    return (
      <nav className='navbar fixed-bottom navbar-light bg-primary'>I am the Footer</nav>
    )
  }
}
